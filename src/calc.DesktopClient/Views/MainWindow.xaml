<Window x:Class="calc.DesktopClient.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    Title="{Binding Title}"
    Width="525"
    Height="350"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ContentControl
            Margin="8"
            prism:RegionManager.RegionName="ToolbarRegion" />
        <ContentControl
            Grid.Row="1"
            Margin="8"
            prism:RegionManager.RegionName="DisplayRegion" />
        <ContentControl
            Grid.Row="2"
            Margin="8"
            prism:RegionManager.RegionName="KeysRegion" />
    </Grid>
</Window>
